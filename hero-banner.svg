<svg width="900" height="300" viewBox="0 0 900 300" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#0A0A0F"/>
      <stop offset="100%" style="stop-color:#13131A"/>
    </linearGradient>
    <linearGradient id="accent-line" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#FF4500"/>
      <stop offset="60%" style="stop-color:#FF7A3D"/>
      <stop offset="100%" style="stop-color:#FF450000"/>
    </linearGradient>
    <filter id="blur-glow">
      <feGaussianBlur stdDeviation="22" result="blur"/>
      <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
    <filter id="leaf-glow">
      <feGaussianBlur stdDeviation="3" result="blur"/>
      <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
    <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
      <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#FF4500" stroke-width="0.4" stroke-opacity="0.14"/>
    </pattern>
    <clipPath id="rounded">
      <rect width="900" height="300" rx="16"/>
    </clipPath>
  </defs>

  <g clip-path="url(#rounded)">
    <rect width="900" height="300" fill="url(#bg)"/>
    <rect width="900" height="300" fill="url(#grid)"/>
    <circle cx="800" cy="-30" r="220" fill="#FF4500" opacity="0.09" filter="url(#blur-glow)"/>
    <circle cx="60"  cy="330" r="180" fill="#FF7A3D" opacity="0.06" filter="url(#blur-glow)"/>
    <rect width="900" height="300" rx="16" fill="none" stroke="#FF4500" stroke-width="1" stroke-opacity="0.3"/>
    <rect x="0" y="0" width="900" height="4" rx="1" fill="url(#accent-line)"/>

    <!-- Status pill -->
    <rect x="48" y="40" width="224" height="30" rx="15" fill="#FF4500" fill-opacity="0.13" stroke="#FF4500" stroke-width="1" stroke-opacity="0.55"/>
    <circle cx="67" cy="55" r="5" fill="#FF4500">
      <animate attributeName="opacity" values="1;0.25;1" dur="2s" repeatCount="indefinite"/>
    </circle>
    <text x="82" y="61" font-family="'Courier New', monospace" font-size="13" font-weight="600" fill="#FF4500" letter-spacing="1">available for work</text>

    <!-- Name -->
    <text x="48" y="138" font-family="Arial Black, Arial" font-size="60" font-weight="900" fill="#FFFFFF" letter-spacing="-1">Mohamed</text>
    <text x="48" y="198" font-family="Arial Black, Arial" font-size="60" font-weight="900" fill="#FF4500"  letter-spacing="-1">Alaa.</text>

    <!-- Accent line -->
    <rect x="48" y="213" width="72" height="4" rx="2" fill="#FF4500"/>

    <!-- Role -->
    <text x="48" y="244" font-family="'Courier New', monospace" font-size="16" font-weight="600" fill="#FFFFFF" letter-spacing="0.4">AI Engineer  &amp;  Backend Developer</text>

    <!-- Tagline -->
    <text x="48" y="270" font-family="'Courier New', monospace" font-size="13" fill="#BBBBBB" letter-spacing="0.3">// Building systems that think and scale</text>

    <!-- Autumn Leaves -->

    <!-- Leaf 1 - large, top right area -->
    <g filter="url(#leaf-glow)" opacity="0.92">
      <text x="720" y="90" font-size="52" font-family="serif" transform="rotate(-25, 746, 72)">🍁</text>
    </g>
    <animateTransform xlink:href="#leaf1" attributeName="transform" type="rotate" from="-25 746 72" to="-20 746 72" dur="3.5s" repeatCount="indefinite" additive="sum"/>

    <!-- Leaf 2 - medium, upper right -->
    <g filter="url(#leaf-glow)" opacity="0.80">
      <text x="820" y="60" font-size="34" font-family="serif" transform="rotate(15, 837, 42)">🍁</text>
    </g>

    <!-- Leaf 3 - small, far right mid -->
    <g filter="url(#leaf-glow)" opacity="0.65">
      <text x="855" y="155" font-size="24" font-family="serif" transform="rotate(30, 867, 143)">🍁</text>
    </g>

    <!-- Leaf 4 - large, bottom right -->
    <g filter="url(#leaf-glow)" opacity="0.88">
      <text x="700" y="238" font-size="46" font-family="serif" transform="rotate(-10, 723, 215)">🍁</text>
    </g>

    <!-- Leaf 5 - medium, bottom far right -->
    <g filter="url(#leaf-glow)" opacity="0.70">
      <text x="808" y="272" font-size="30" font-family="serif" transform="rotate(20, 823, 257)">🍁</text>
    </g>

    <!-- Leaf 6 - tiny accent, corner -->
    <g filter="url(#leaf-glow)" opacity="0.50">
      <text x="858" y="210" font-size="18" font-family="serif" transform="rotate(-40, 867, 201)">🍁</text>
    </g>

    <!-- Leaf 7 - medium floating mid-right -->
    <g filter="url(#leaf-glow)" opacity="0.75">
      <text x="770" y="170" font-size="28" font-family="serif" transform="rotate(10, 784, 156)">🍁</text>
    </g>

    <!-- Leaf 8 - small top far right -->
    <g filter="url(#leaf-glow)" opacity="0.55">
      <text x="876" y="100" font-size="20" font-family="serif" transform="rotate(-15, 886, 90)">🍁</text>
    </g>

  </g>
</svg>
